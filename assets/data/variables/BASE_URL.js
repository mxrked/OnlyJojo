// Easier way for changing HTTP and makes the base url a variable

const HTTP_TYPE = "http://";
const SERVER = "basicallyeasy.com/NextJSCDNS/onlyjojo/";

const BASE_URL = HTTP_TYPE + SERVER;

export default BASE_URL;
